#include<stdio.h>
int main()
{
    int n;
   
    scanf("%d", &n);
    int a[1000]
    
	for (int i=0; i<n; i++) {
		scanf("%d", &a[i]);
	int max= a[0];
	
    	if (a[i]> max ) {
    		max =a[i]; }
    }
    printf("%d", a[i];  

    		
    
    
        
    return 0;
}
